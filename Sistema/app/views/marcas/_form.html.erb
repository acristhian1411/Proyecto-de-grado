<%= form_with(model: marca, local: true) do |form| %>
  <% if marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marca.errors.count, "error") %> prohibited this marca from being saved:</h2>

      <ul>
      <% marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :marca_descrip %>
    <%= form.text_field :marca_descrip %>
  </div>

  <div class="field">
    <%= form.label :marca_active %>
    <%= form.check_box :marca_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
